Chapter 1 : Math Foundations — Numbers, Vectors & Similarity
│
├── Scalars & Vectors [done]
├── Matrix & Matrices [done]
├── Dot Product [done]
└── Matrix & Matrices Multiplication 
    ├── Matrix multiplication : (Matrix × vector, Matrix × matrix) [done]
    └── Matrix multiplication as table lookups / projection
        └── Matrix multiplication as a table lookup [done]
└── Cosine Similarity
    └── Explain why embeddings use cosine similarity ?

↓

Chapter 2 : Text to Math — Tokenization & Representations
│
├── Text Processing (Lemmatization, Stemming, Character-level)
├── Vocabulary & Tokenization
└── Discrete Representations (One-Hot Encoding)
    ├── One-hot encoding
    └── Bridge to continuous space (Vector Embeddings)

↓

Chapter 3 : Sequence Modeling — Classical Approaches
│
├── First-order Sequence Models (Markov Chains)
│   └── First order sequence model
├── Second-order Sequence Models
│   └── Second order sequence model
└── Skip Models & Matrix Multiplications
    ├── Second order sequence model with skips
    ├── Second order sequence model as matrix multiplications
    └── Sequence completion & next-token prediction
        └── Sequence completion

↓

Chapter 4 : Limitations of Classical Sequence Models
│
├── Long-range Dependencies
├── Information Bottlenecks
└── Why Markov Models / RNNs Struggle (Sequential processing constraints)
    └── Rest Stop and an Off Ramp

↓

Chapter 5 : Attention Mechanisms
│
├── The Core Idea: Bypassing the bottleneck
├── Queries, Keys, and Values (Q, K, V)
├── Attention Scores (Dot Product Similarity applied to Q & K)
├── Attention as Matrix Multiplication (Table lookup at scale)
│   └── Attention as matrix multiplication
└── Masking (Causal / Autoregressive Attention)
    └── Masking


tensors